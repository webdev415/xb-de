package p000;

import java.util.concurrent.Executor;

public final class ExecutorC0486Kg implements Executor {
    @Override
    public final void execute(Runnable runnable) {
        runnable.run();
    }
}
